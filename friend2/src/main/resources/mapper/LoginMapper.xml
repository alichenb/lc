<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyust.friend.core.dao.LoginDao">
    <!-- 查询单条记录 -->
    <select id="getUserById" parameterType="string" resultType="com.tyust.friend.core.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

</mapper>

